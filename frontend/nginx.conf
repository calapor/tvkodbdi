server {
    listen 80;
    server_name _;

    resolver 10.43.0.10 valid=10s ipv6=off;
    root /usr/share/nginx/html;
    index index.html;

    # Serve React app files
    location / {
        try_files $uri /index.html;
    }


  # Proxy API requests to backend serviceserver {
                                           #     listen 80;
                                           #     server_name _;
                                           #
                                           #     root /usr/share/nginx/html;
                                           #     index index.html;
                                           #
                                           #     # Serve React app files
                                           #     location / {
                                           #         try_files $uri /index.html;
                                           #     }
                                           #
                                           #
                                           #   # Proxy API requests to backend service
                                           #   location /api/ {
                                           #     proxy_pass http://tvkodbdi-backend-service.default.svc.cluster.local:3000/;
                                           #     proxy_http_version 1.1;
                                           #     proxy_set_header Upgrade $http_upgrade;
                                           #     proxy_set_header Connection 'upgrade';
                                           #     proxy_set_header Host $host;
                                           #     proxy_cache_bypass $http_upgrade;
                                           #
                                           #     # Optional: Extended timeouts for APIs
                                           #     proxy_connect_timeout   180s;
                                           #     proxy_send_timeout      180s;
                                           #     proxy_read_timeout      180s;
                                           #     send_timeout            180s;
                                           #   }
                                           #
                                           # }
  location /api/ {
    # proxy_pass http://tvkodbdi-backend-service.default.svc.cluster.local:3000/;
    # Use a variable — this forces dynamic resolution instead of startup-time
    set $backend "tvkodbdi-backend-service.default.svc.cluster.local";
    proxy_pass http://$backend:3000;    
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_cache_bypass $http_upgrade;

    # Optional: Extended timeouts for APIs
    proxy_connect_timeout   180s;
    proxy_send_timeout      180s;
    proxy_read_timeout      180s;
    send_timeout            180s;
  }

}

